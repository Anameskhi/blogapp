<section class="vh-100 bg-image"
  style="background-color: CadetBlue">
  <div class="mask d-flex align-items-center h-100 gradient-custom-3">
    <div class="container h-100">
      <div class="row d-flex justify-content-center align-items-center h-100">
        <div class="col-15 col-md-10 col-lg-15 col-xl-20">
          <div class="card" style="border-radius: 15px;">
            <div class="card-body p-5">
           

<div class = "container">
      
<p style="color: green"><%= notice %></p>

<%= render @place %>


<% if (current_user && current_user.id == @user.id) || current_user&.admin? %>
  <%= link_to "Edit this place", edit_place_path(@place) %> |
   <%= button_to "Destroy this place", @place, method: :delete %>
   <%end%>
  
<div  class="text-center">
  <%= image_tag "https://maps.googleapis.com/maps/api/staticmap?zoom=17&size=400x300&center=#{@place.latitude},#{@place.longitude}&key=#{Rails.application.credentials[:MAPS_API_KEY]}",class:"rounded  ",  alt: 'Map'%>
</div><br>

  <div class="row d-flex justify-content-center  ">
  <%= link_to "Back to adresses", user_places_path, class: "btn btn-outline-success btn-block btn-lg gradient-custom-4 "  %>
</div>
</div>
 </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>