<div class = "container text-center">
  <p>
  <%= link_to t("admin"), admin_path, class: "btn btn-danger" %> 
 <strong><%=  t("↭ #{params[:action].capitalize}") unless params[:action].eql?("index")%></strong>
  </p>
  <div class="btn-group">
  <%= render "admin/link", resource:Post %>
  <%= render "admin/link", resource:Comment %>
  <%= render "admin/link", resource:User %>
  </div>
 
</div>