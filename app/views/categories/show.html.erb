<div class= "container">
    <div class="row justify-content-md-center">
         <div class="col-8 mt-4">
            <div class="card text-center shadow mb-5 bg-white rounded">
<p style="color: green"><%= notice %></p>




<% if current_user&.admin? %>
<div class="btn-group">
  <%= link_to t("edit_this_category"), edit_category_path(@category), class:'btn btn-secondary' %> 
  <%= button_to t("destroy_this_category"), @category, method: :delete, class:'btn btn-danger' %>
</div>
<%end%>
<%= render @category %>
<div>
  <%= link_to t("back_to_categories"), categories_path, class: "btn btn-outline-primary " %>
  
</div>
  <%= pagy_bootstrap_nav(@pagy).html_safe%>
</div>
</div>
</div>
</div>

